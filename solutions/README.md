# solutions/

This is where the brand-new daily builds live — starting **Day 1: Own Random, Own Luck**.

Each day gets its own folder (e.g. `day-01-dice/`), and every day ends with a commit:
`git add -A` then `git commit -m "Day N: <what you built>"`.

Old coursework was deleted — everything here is written fresh, by you, from Day 1 forward.